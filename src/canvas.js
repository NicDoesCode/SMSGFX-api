// const { createCanvas, loadImage } = require('canvas');

const makeImage = async () => {
    // const canvas = createCanvas(640, 480);
    // // const canvas = new OffscreenCanvas(640, 480);
    // const context = canvas.getContext('2d');
    // context.fillStyle = '#0000AA';
    // context.fillRect(0, 0, canvas.width, canvas.height);
    // context.fillStyle = '#00AAAA';
    // context.fillRect(100, 100, canvas.width - 200, canvas.height - 200);
    // context.strokeStyle = '#000000';
    // context.strokeText('Hello from Node!', 250, 250);

    // const data = await canvas.convertToBlob({ type: 'image/png' });

    // const path = require('path');
    // const directory = path.dirname(require.main.filename);
    
    // const filePath = path.join(directory, 'example.png');

    // const fs = require('fs');
    // fs.writeFileSync(filePath, data);
}

module.exports = { makeImage };